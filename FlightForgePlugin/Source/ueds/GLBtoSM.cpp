#include "GLBtoSM.h"

void AGLBtoSM::BeginPlay() {
    Super::BeginPlay();

    /* UStaticMesh* MyMesh = GLBtoSM("C:\\Users\\bastl\\Downloads\\monkey.glb"); */

    /* if (MyMesh) { */
    /*     UE_LOG(LogTemp, Warning, TEXT("Got a valid static mesh from blueprint.")); */
    /* } else { */
    /*     UE_LOG(LogTemp, Warning, TEXT("Static mesh is null.")); */
    /* } */

    /* UE_LOG(LogTemp, Warning, TEXT("Hello from CPP!")); */
}
